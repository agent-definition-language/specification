name: PR title

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  contents: read

jobs:
  title:
    uses: agent-definition-language/.github/.github/workflows/pr-title-lint.yml@main
